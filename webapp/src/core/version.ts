/** Build-time version constants (no async provider needed on web). */

export const appVersion = '0.2.0-web';
export const appBuildNumber = '0';

export const versionShortLabel = `v${appVersion}`;
export const versionFullLabel = `v${appVersion} (${appBuildNumber})`;
