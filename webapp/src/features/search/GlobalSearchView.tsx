import { PendingSubPage } from '../common/PendingPages';

/** /menu/search — global search across maps, KB, jobs, wallets, notes. */
export function GlobalSearchView() {
  return <PendingSubPage title="Search" />;
}
