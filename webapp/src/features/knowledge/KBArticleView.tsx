import { PendingSubPage } from '../common/PendingPages';

/** /knowledge/article/:slug — a knowledge base article (markdown). */
export function KBArticleView() {
  return <PendingSubPage title="Article" />;
}
