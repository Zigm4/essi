import { PendingTabPage } from '../common/PendingPages';

/** /knowledge — knowledge base home (Knowledge tab). */
export function KBHomeView() {
  return <PendingTabPage bannerLabel="ESSI · Knowledge Core" />;
}
