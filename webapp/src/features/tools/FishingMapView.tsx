import { PendingSubPage } from '../common/PendingPages';

/** /tools/fishing — 96 zones + 4 map rooms, depths & poles. */
export function FishingMapView() {
  return <PendingSubPage title="Fishing Map" />;
}
