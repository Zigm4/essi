import { PendingSubPage } from '../common/PendingPages';

/** /tools/scan — live planet positions (JPL Horizons via the configured proxy). */
export function SystemScanView() {
  return <PendingSubPage title="System Scan" />;
}
