import { PendingSubPage } from '../common/PendingPages';

/** /tools/discoveries — find comets and asteroids by date (NASA SBDB). */
export function CelestialView() {
  return <PendingSubPage title="Discoveries" />;
}
