import { PendingSubPage } from '../common/PendingPages';

/** /tools/jobs — search 371 jobs by faction, reward, skill, location. */
export function JobsView() {
  return <PendingSubPage title="Jobs" />;
}
