import { PendingSubPage } from '../common/PendingPages';

/** /tools/mars-express — live schedule + zone alerts (alerts are mobile-only). */
export function MarsExpressView() {
  return <PendingSubPage title="Mars Express" />;
}
