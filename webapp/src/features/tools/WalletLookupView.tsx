import { PendingSubPage } from '../common/PendingPages';

/** /tools/wallet — query param ?q pre-seeds the lookup (read it when implemented). */
export function WalletLookupView() {
  return <PendingSubPage title="Wallet Lookup" />;
}
