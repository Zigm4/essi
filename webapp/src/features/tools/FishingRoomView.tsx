import { PendingSubPage } from '../common/PendingPages';

/** /tools/fishing/:roomId — a single fishing map room. */
export function FishingRoomView() {
  return <PendingSubPage title="Fishing Room" />;
}
